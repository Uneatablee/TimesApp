cmake_minimum_required(VERSION 3.30)

project(DayPlanner)

set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED True)

add_compile_options(-Wall -Wextra)

#add subdirectories

enable_testing()

add_subdirectory(tests)
add_subdirectory(src/business_logic/DayPlanner)

include_directories(FakeIt/include)


#Switch to QT project after making basic calendar business logic with working date, time, events and tasks

#Implement QT Cmake file so it links with headers from QT window, widgets and ui.
#   ---Different implementation for main for business logic testing
#   ---Different implementation for mid-level ui before switching to electron frontend

#Adding Cmake compilation and linking options for Windows OS